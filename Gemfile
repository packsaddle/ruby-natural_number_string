source 'https://rubygems.org'

# Specify your gem's dependencies in integral_string.gemspec
gemspec

group :development do
  gem 'rubocop', group: :test
  gem 'byebug', group: :test if RUBY_VERSION >= '2.0.0'
  gem 'parse_gemspec-cli'
end
